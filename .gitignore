node_modules/
coverage/

# Working notes and agent instructions for this repository. Written for whoever
# is building the plugin, not for the people installing it.
notes/
.claude/

# Build output. Obsidian loads it, a release attaches it, but it is generated
# from src/ and never edited by hand - so it does not belong in git.
main.js
main.js.map

.DS_Store
