min_version = "2026.8.8"

[settings]
experimental = true # required for [deps]
lockfile = true

[env]
_.path = ["{{config_root}}/node_modules/.bin"]
HK_MISE = 1

[task_config]
# Listing includes replaces the defaults, so .mise/tasks has to be named too.
includes = [".mise/tasks"]

[deps.node]
auto = true
sources = ["package.json", "package-lock.json"]
outputs = ["node_modules/.bin/eslint"]
run = "npm install"

[hooks]
enter = ["mise install", { run = "mise deps" }]
postinstall = ["hk install --mise"]

# Owned by flint: `flint run --fix flint-setup` rewrites this table.
[tools]
hk = "1.58.1"
node = "24.20.0"

"npm:doctoc" = "2.5.0"

# Linters
actionlint = "1.7.12"
"aqua:grafana/flint" = "0.22.11"
"aqua:owenlamont/ryl" = "0.22.0"
biome = "2.5.12"
rumdl = "0.2.68"
shellcheck = "0.11.0"
taplo = "0.10.0"
zizmor = "1.30.0"

[tasks.lint]
description = "Every generic file type — shell, markdown, yaml, toml, js/json, workflows"
run = "flint run --full"

[tasks.fmt]
description = "Apply what flint can fix, report the rest"
run = "flint run --full --fix --allow-fixed"

[tasks.check]
description = "Everything that gates a push"
depends = [
  "lint",
  "lint:code",
  "test:types",
  "test:coverage",
  "lint:docs",
  "test:dead",
  "test:release",
  "test:strings",
  "test:styles",
  "test:scanner",
  "test:manifest",
  # Last: the production build is what a release attaches, and a gate that never
  # builds lets a release ship without one.
  "build",
]
